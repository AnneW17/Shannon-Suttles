---
/**
 * DRAFT PREVIEW — currently disabled.
 *
 * This route was server-rendered, which is why the site carried the Netlify
 * adapter and a serverless function in front of every page. It has never
 * actually worked: it needs SANITY_VIEWER_TOKEN, and that was never set in
 * Netlify's environment variables.
 *
 * Returning no paths means the route generates nothing and the site builds
 * fully static — no function, no cold starts, no blank page on navigation.
 *
 * To switch preview back on:
 *   1. Create a read-only "Viewer" token at sanity.io/manage
 *   2. Add it to Netlify as SANITY_VIEWER_TOKEN
 *   3. Reinstate `adapter: netlify()` in astro.config.mjs
 *   4. Restore this file from git history
 */
export async function getStaticPaths() {
  return [];
}
---
